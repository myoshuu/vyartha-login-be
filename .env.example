# @note Database connection URL (MySQL)
# Format: mysql://user:password@host:port/database
# If using Docker, replace 'host' with your MySQL container name or internal IP
DATABASE_URL=mysql://root:password@localhost:3306/growtopia

# @note Server port
PORT=3000
